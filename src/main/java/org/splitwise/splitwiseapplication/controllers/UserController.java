package org.splitwise.splitwiseapplication.controllers;

public class UserController {
    /*
     should have user's behaviour, http methods
     register(), updateUserProfile(), login(), signUp(), logOut(),
     */
}
